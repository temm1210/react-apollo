{
  "name": "portfolio",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "author": "STW",
  "scripts": {
    "start": "yarn workspace back start",
    "build": "yarn workspace front build && yarn workspace front build-ssr"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ],
    "nohoist": [
      "**/react-scripts",
      "**/react-scripts/**"
    ]
  },
  "private": true,
  "engines": {
    "node": "10.x"
  }
}
